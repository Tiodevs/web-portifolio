# Dockerfile.dev - Para desenvolvimento (com hot reload)

# Usa a imagem base do Node.js
FROM node:18-alpine

# Instala dependências do sistema
RUN apk add --no-cache libc6-compat

# Define diretório de trabalho
WORKDIR /app

# Copia arquivos de dependências
COPY package*.json ./

# Instala dependências (incluindo devDependencies)
RUN npm ci

# Expõe a porta 3000
EXPOSE 3000

# Comando para rodar em modo desenvolvimento
# O código será montado via volume, então não precisamos copiar aqui
CMD ["npm", "run", "dev"] 